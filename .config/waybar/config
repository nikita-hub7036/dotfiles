{
  "position": "top",
  "margin-top": 2,
  "margin-left": 10,
  "margin-right": 10,
  "height": 28,

  // Левый блок
  "modules-left": [
    "sway/workspaces",
    "sway/window"
  ],

  // Центральный блок
  "modules-center": [
    "clock"
  ],

  // Правый блок
  "modules-right": [
    "sway/language",
    "pulseaudio",
    "battery",
    "network",
    "temperature",
    "custom/disk",
    "tray"
  ],

 "sway/window": {
  "format": "{}",
  "max-length": 25,
  "rewrite": {
    "Mozilla Firefox": "Firefox",
    "Visual Studio Code": "VS Code",
    "Alacritty": "Terminal",
    "": "Sway"
  },
  "separate-outputs": true}, 

  "sway/language": {
    "format": "{short}",        // будет показывать en, ru и т.п.
    "tooltip": false
  },

  "sway/workspaces": {
    "format": "{icon}",
    "on-click": "activate",
    "format-icons": {
      "active": " "
    },
    "sort-by-number": true,
  },

    "clock": {
    "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>",
    "format-alt": "{:%Y-%m-%d}"
  },

 
  "cpu": {
    "format": "{icon}   {usage}%",
    "tooltip": false,
    "format-icons": ""
  },

  "memory": {
    "format": "{icon}   {percentage}%",
    "format-icons": "󰘚"
  },

  "temperature": {
    "critical-threshold": 80,
    "format": "{temperatureC}°C",
    "format-icons": [
      "",
      "",
      ""
    ]
  },

  "battery": {
    "states": {
      "warning": 30,
      "critical": 15
    },
    "format": "{icon} {capacity}%",
    "format-full": "{icon} {capacity}%",
    "format-charging": " {capacity}%",
    "format-plugged": " {capacity}%",
    "format-alt": "{time} {icon}",
    "format-icons": [
      "",
      "",
      "",
      "",
      ""
    ]
  },

  "network":{
    "format-wifi": " {signalStrength}%",
    "format-ethernet": "{cidr} ",
    "tooltip-format": "{ifname} via {gwaddr} ",
    "format-linked": "{ifname} (No IP) ",
    "format-disconnected": " ⚠ ",
    "format-alt": "{ifname}: {ipaddr}/{cidr}"
  },

 

"custom/brightness": {
  "exec": "bash -c 'cur=$(brightnessctl g); max=$(brightnessctl m); echo $((cur * 100 / max))'",
  "format": " {}%",
  "interval": 60,
  "signal": 2,
  "tooltip": false
},



  "pulseaudio": {
    "format": "{icon} {volume}%",
    "format-bluetooth": "{icon} {volume}%",
    "format-bluetooth-muted": " {icon} {format_source}",
    "format-muted": " {format_source}",
    "format-icons": {
      "headphone": "",
      "hands-free": "",
      "headset": "",
      "phone": "",
      "portable": "",
      "car": "",
      "default": [
        "",
        "",
        ""
      ]
    },
    "on-click": "pavucontrol"
  },

  "tray": {
    "icon-size": 22,
    "spacing": 10
    }, 

 "custom/disk": {
  "exec": "df -h /home | awk 'NR==2{print $3\"/\"$2}'",
  "interval": 60,
  "tooltip": false,
  "format": " {}"
},


  "custom/player": {
    "exec": "playerctl metadata --format '{{ title }}'",
    "interval": 5,
    "max-length": 30,
    "on-click": "playerctl play-pause",
    "on-scroll-up": "playerctl next",
    "on-scroll-down": "playerctl previous",
    "return-type": "string",
    "format": "   {}"
  },

  "custom/uptime": {
    "exec": "uptime -p | sed 's/up //' | sed 's/hours*/h/' | sed 's/minutes*/m/' | sed 's/,//g'", 
    "interval": 60,
    "tooltip": false,
    "format": "{}"
  },

  "custom/power": {
  "format": "",
  "tooltip": false,
  "on-click": "wlogout"
  }

}

